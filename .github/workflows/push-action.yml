name: First github action
on:
  push:
    branches: [ main ]
    
jobs:
  job_one:
    name: make a ls
    runs-on: ubuntu-latest
    steps:
      - name: ssh connection
        uses: appleboy/ssh-action@master
        with:
          host: 92.243.20.86
          port: 22
          username: dev
          key: ${{ secrets.SECRET_KEY }}
          script: |
            ls
